import { WebsocketExceptionsFilter } from './websocket-exception.filter';

describe('WebsocketExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new WebsocketExceptionsFilter()).toBeDefined();
  });
});
